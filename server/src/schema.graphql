type Query {
  hello: String!
}

type Mutation {
  # prettier-ignore
  signup(firstName: String! lastName: String! email: String! password: String!): [Error!]
}

# Utils
type Error {
  path: String!
  message: String!
}
